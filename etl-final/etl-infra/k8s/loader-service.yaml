apiVersion: v1
kind: Service
metadata:
  name: loader-service
  namespace: etl
spec:
  selector:
    app: loader-service
  ports:
    - name: http
      port: 8005
      targetPort: 8005
    - name: health
      port: 8085
      targetPort: 8085
