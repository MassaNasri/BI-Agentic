apiVersion: apps/v1
kind: Deployment
metadata:
  name: connector-service
  namespace: etl
spec:
  replicas: 2
  selector:
    matchLabels:
      app: connector-service
  template:
    metadata:
      labels:
        app: connector-service
    spec:
      containers:
        - name: connector-service
          image: etl-final/connector-service:latest
          ports:
            - containerPort: 8001
          envFrom:
            - configMapRef:
                name: etl-config
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 10
            periodSeconds: 20
