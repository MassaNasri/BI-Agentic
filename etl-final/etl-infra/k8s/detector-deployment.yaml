apiVersion: apps/v1
kind: Deployment
metadata:
  name: detector-service
  namespace: etl
spec:
  replicas: 2
  selector:
    matchLabels:
      app: detector-service
  template:
    metadata:
      labels:
        app: detector-service
    spec:
      containers:
        - name: detector-service
          image: etl-final/detector-service:latest
          ports:
            - containerPort: 8002
          envFrom:
            - configMapRef:
                name: etl-config
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 20
