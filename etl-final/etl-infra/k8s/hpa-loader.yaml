apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: loader-service
  namespace: etl
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: loader-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
